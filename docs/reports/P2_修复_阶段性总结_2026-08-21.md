# P2 全量修复（B方案）— 阶段性总结 2026-08-21

> 状态：**根因确认 ✅ / SQL 生成 ✅ / 执行启动 ⚠️（结果待明天确认）**
> 背景：切割附魔"不触发"排查 → P2 池 17.8% 词缀无效（learn 型 2439 + [RC] trigger DUMMY 609 + X缺失 16）

## 1. 根因（已坐实，内核源码级）
- **learn 型 2439 条**：SIE 指向 effect36 原始法术 → 装备教技能（主动技能不自动触发 + 职业限制）
- **[RC] trigger DUMMY 609 条**：[RE] 触发法术是 effect1=3 DUMMY，SpellEffects.cpp 无 DUMMY case → proc 空转
- **learn X 缺失 16 条**：learn 目标法术在 spell_template 不存在

## 2. 修复方案（B，已定稿且职业安全三重保障）
| # | 处理 | 数量 | 方式 |
|---|---|---|---|
| 1 | learn 被动 → 光环化 | 1261 | UPDATE A：effect36→6 + 复制 X 光环参数（装备即光环生效） |
| 2 | learn 主动 → 容器化 | 1162 | UPDATE A：effect36→aura42 + trigger X + duration21 + **INSERT spell_proc_event**(0x10154/15%/CD1000) |
| 3 | learn X 缺失 → 剔除 | 16 | 权重表重建（17249→**17233** 行，IRP 映射保持 52001+旧下标） |
| 4 | [RC] trigger DUMMY → X | 609 | UPDATE [RC].effectTriggerSpell1 = 原始法术 X（0 失败） |

**职业安全（用户着重关注）**：
1. **原始职业技能 X 零修改**（玩家正常技能完全不受影响）
2. 内核 `UnitAuraProcHandler.cpp:1728` 确认：proc 触发 CastSpell(triggered=true) **免职业/免姿态/免法力消耗**，正向法术自动对自己施放
3. 已学会技能不消失（仅新装备不再教）；本次为**纯 DB 修复，不动 SIE/IRP DBC、不需重压 patch-A、不需重开客户端**

## 3. 产物（outputs/）
- `p2_fix_aura.sql`（1261 UPDATE）/ `p2_fix_container.sql`（1162 UPDATE + 1162 proc_event）/ `p2_fix_rc.sql`（609 UPDATE）/ `p2_weight_rebuild.sql`（17233 行）
- `p2_drop16.csv`（16 条 X 缺失清单）/ `p2_fix_report.txt`（生成报告）
- `p2_fix_gen_remote.py`（远端生成器，pymysql 直连 tw_pb_world）/ `p2_fix_exec.py`（执行脚本：备份→4 SQL→校验→restart）

## 4. ⚠️ 执行状态（明天首要确认）
- `p2_fix_exec.py` 于 17:10 启动，**20+ 分钟无输出仍在后台跑**（可能卡在备份 CREATE TABLE AS SELECT 或 SSH channel），**执行结果未知**。
- 明天第一步：确认远端状态——
  ```bash
  # 备份表是否已建（= 执行已开始）
  SELECT COUNT(*) FROM tw_pb_world.spell_template_bak_20260821_p2fix;
  # learn 残留（0 = 修复已生效）
  SELECT COUNT(*) FROM tw_pb_world.spell_template
  WHERE (effect1=36 OR effect2=36 OR effect3=36)
  AND entry IN (SELECT spell_id FROM tw_pb_world._p2_sie);
  # 权重表（17233 = 已重建）
  SELECT COUNT(*) FROM tw_pb_world.item_enchantment_template WHERE entry=52001;
  ```
- 若未执行/部分执行：先查 SHOW PROCESSLIST（可能有卡住的 CREATE TABLE AS SELECT 锁），kill 后重跑 `p2_fix_exec.py`（脚本含备份，幂等安全）；执行顺序 aura → container → rc → weight。

## 5. 明天步骤清单
1. **确认执行结果**（上表 3 条 SQL）；未生效则排查/重跑
2. **restart pbmangosd** + journalctl/server log 确认无 IRP/法术错误
3. **游戏内验证**（重开客户端可选，服务端法术已生效）：
   - 切割（之切割）：攻击触发攻速 buff（不再"学会技能"）
   - 背刺/斩杀（容器化）：攻击 15% 触发
   - 耐力/专精（光环化）：装备即生效，无学习提示
   - 非盗贼角色穿切割武器：proc 正常触发攻速（职业安全验证）
4. **回归**：随机刷 20 件绿装，确认全部词缀有实际效果（无白板/无空壳）
5. 落盘《P2 修复完成报告》

## 6. 回滚（若需要）
```sql
-- 备份表（若已建）
DROP TABLE IF EXISTS spell_template;  -- 危险，勿直接执行！
-- 正确回滚：从备份表恢复
DELETE FROM spell_template;
INSERT INTO spell_template SELECT * FROM spell_template_bak_20260821_p2fix;
DELETE FROM spell_proc_event;
INSERT INTO spell_proc_event SELECT * FROM spell_proc_event_bak_20260821_p2fix;
DELETE FROM item_enchantment_template WHERE entry=52001;
INSERT INTO item_enchantment_template SELECT * FROM item_enchantment_template_bak_20260821_p2fix WHERE entry=52001;
-- + restart pbmangosd
```
> 若备份表未建立（执行未开始），直接重跑脚本即可，无需回滚。
