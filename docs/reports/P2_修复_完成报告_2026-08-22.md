# P2 全量修复 — 完成报告 2026-08-22

> 状态：**已全部落地 ✅（DB + 服务重启 + 日志验证）**
> 上游：`P2_修复_阶段性总结_2026-08-21.md`（昨晚执行部分生效，今日补完）

## 0. 最终结论
P2 池 52001 词缀现为 **16716 条全有效词缀**（权重 SUM=100.000000），无白板、无空壳、无死链。
learn 型 2439（光环化 1261 + 容器化 1162 + X缺失剔除 16）与 [RC] trigger DUMMY 609 全部处置完毕。

---

## 1. 昨晚执行复盘（2026-08-21 17:10 启动的 p2_fix_exec.py）
| 步骤 | 昨晚实际 | 证据 |
|---|---|---|
| 备份 3 表 | ✅ 已建（spell_template_bak 69363 行等） | 探针确认 |
| aura.sql（learn 光环化 1261） | ✅ 生效 | learn 残留 1178 = 2439−1261 |
| container.sql（1162 容器化 + proc_event） | ❌ 未生效 | proc_event bak=now=15438；weight.sql 未上传 /tmp |
| rc.sql（609 trigger→X） | ⚠️ 执行但**修复本身失效** | 见 §2 |
| weight_rebuild.sql | ❌ 未执行 | 权重仍在 17249 |
| restart pbmangosd | ✅ 已重启（17:10:48） | ActiveEnterTimestamp |

> 推断：脚本在 container 之后中断（channel 超时类异常），weight.sql 未及上传。今晨重跑 3 个 SQL，全部 RC=0 成功。

## 2. 今日关键发现：rc.sql 的"同名 X"修复对 517/609 无效
**背景**：[RC] 附魔 = effect1=6 + aura42(PROC_TRIGGER_SPELL) + trigger→[RE] 壳（DUMMY）。原方案把 trigger 改为"去 [RE] 前缀的同名法术 X"。

**实证**（逐条核对 609 条）：
- rc.sql 609 条 UPDATE **全部执行成功**（trigger 均已 = X）
- 但抽查 X 本身：`2400 Transfer Power / 2950 Anti-Holy / 8026 Flametongue Weapon Proc / 22913 Random Aggro` 等 **effect1=3 全 DUMMY**
- 死链 trigger 443 个 unique 在 `spell_scripts` 绑定数 = **0**（[RE] 段 300000+ 绑定 = 0）
- 即：**把 trigger 从"[RE] DUMMY"改成"同名 DUMMY"= 修复无效**；仅 92 条 X 非 DUMMY 真正修好

**结论**：这些 DUMMY 在 Turtle 内核（SpellEffects.cpp 无 DUMMY case）+ 无脚本 = proc 必然空转。**无法通过改法找有效效果（同名法术本身就是 DUMMY），只能从权重池剔除。**

## 3. 今日执行的修复（全部已验证）
| # | 处理 | 数量 | 方式 | 状态 |
|---|---|---|---|---|
| 1 | container.sql 重跑（learn 主动容器化 + proc_event） | 1162 + 1162 | UPDATE aura42/trigger X/duration21 + INSERT proc_event(0x10154/15%/1000) | ✅ RC=0 |
| 2 | rc.sql 重跑（[RC] trigger→X） | 609 | UPDATE effectTriggerSpell1=X（92 条有效，517 条仍死链） | ✅ RC=0 |
| 3 | weight_rebuild.sql 重跑（X 缺失 16 剔除） | 17249→17233 | DELETE+INSERT 等权 0.00580282 | ✅ |
| 4 | **死链剔除（今日新增）** | **17233→16716** | DELETE 517 死链 IRP + chance 归一化 0.0059822924 | ✅ |
| 5 | restart pbmangosd（systemd） | - | 新日志 server_2026-08-22_00-41-05.log | ✅ active |

**死链判定标准**（严谨校验）：trigger 法术三槽 effect ∈ {0,3}（DUMMY 无 case）+ `spell_scripts` 无绑定 ⇒ proc 空转 ⇒ 剔除。
- 死链 517 = rc.sql 已改但 X 仍 DUMMY 的 491 + 非 [RE] 命名漏网的 26（2868→2866 "Spell - Reuse" 等）
- 粗筛残留 580 中 **65 条实为有效**（如 trigger 5171 Slice and Dice e2=6 光环、5308 Execute→26651）——**保留**，修正了残留查询误报

## 4. 终态数据（2026-08-22 08:45 快照，重启后）
| 指标 | 值 | 判定 |
|---|---|---|
| item_enchantment_template entry=52001 | **16716 行**，SUM=99.999999 | ✅ 等权归一化 |
| learn(36) 残留（_p2_sie 内） | 16（=X 缺失剔除项，池内无引用） | ✅ 预期 |
| spell_proc_event（_p2_sie 相关） | 15793（基线 15438 + 容器 355 新增） | ✅ |
| 死链行在权重池内 | **0**（交叉验证 sie↔IRP↔pool） | ✅ |
| drop16 sie 在权重池内 | 0 | ✅ |
| 启动日志 IRP/SIE/Enchantment 错误 | **0** | ✅ |
| pbmangosd | active（00:42:56 UTC 启动） | ✅ |

**不变项**：SIE/IRP DBC 未动、客户端 patch-A 无需重压、免职业安全三重保障不变。

## 5. 游戏内验证清单（请用户在 PB 服执行）
无重开客户端要求（服务端 DB 法术已生效）。验证指令无前导点：
1. **切割（之切割）**：攻击触发攻速 buff（不再出现"学会技能"提示）
2. **背刺/斩杀（容器化）**：攻击 15% 触发对应技能效果
3. **耐力/专精（光环化）**：装备即生效，无学习提示
4. **非盗贼穿切割武器**：proc 正常触发攻速（职业安全）
5. **随机刷 20 件绿装**：确认全部词缀有实际效果（权重池已无白板/空壳）

## 6. 回滚（如需）
```sql
-- spell_template / proc_event / 权重 均可从备份表恢复（备份表保留中）：
DELETE FROM spell_template;
INSERT INTO spell_template SELECT * FROM spell_template_bak_20260821_p2fix;
DELETE FROM spell_proc_event;
INSERT INTO spell_proc_event SELECT * FROM spell_proc_event_bak_20260821_p2fix;
DELETE FROM item_enchantment_template WHERE entry=52001;
INSERT INTO item_enchantment_template SELECT * FROM item_enchantment_template_bak_20260821_p2fix WHERE entry=52001;
-- + restart pbmangosd
```
> 注意：备份是 SQL 执行前的状态（17249 池）；回滚后需重跑全部修复。

## 7. 产物（outputs/）
- `p2_weight_purge.sql`（死链 517 剔除 + 归一化）/ `p2_dead_517.csv`（死链清单含词缀名）/ `p2_final_dead.txt`（当前库死链明细）
- `p2_probe_status.py` / `p2_diag*.py` / `p2_classify2.py` / `p2_final_check.py`（本日验证脚本）
- 备份表：`spell_template_bak_20260821_p2fix` / `spell_proc_event_bak_20260821_p2fix` / `item_enchantment_template_bak_20260821_p2fix`（tw_pb_world，保留）