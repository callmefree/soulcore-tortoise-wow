# 切换电脑交接清单（2026-08-09 16:10）

> 本文件用于在**本机 ↔ 家里电脑**之间无缝切换工作。切换到新机器时：先读本文件 + `部署与Eluna移植工作记录.md`，按底部「恢复步骤」操作。

## 一、当前进度状态（截至 2026-08-09 16:07）

**Eluna 第二阶段批次 1-3 全部完成并验证闭环 ✅**

| 里程碑 | 状态 |
|---|---|
| Eluna 引擎集成（tortoise-wow + WYTurtle 移植） | ✅ CI 双平台编译通过（commit `35e439d`） |
| `.reload eluna` 热重载命令 | ✅ 运行时实测通过 |
| 炉石无施法动作残留修复（SpellHandler:174） | ✅ 施法事实已阻止（抬手为客户端本地动画，可接受） |
| 自定义 NPC 190001 + Lua gossip 菜单 | ✅ 打开/防叠加/选项分发全通 |
| 登录欢迎（welcome.lua 事件14） | ✅ |
| 3 个运行时修复（无需重编译） | ✅ 见下 |

## 二、本地数据库改动（SQL 已版本化，无需手动抄写）

**所有本地 SQL 改动已落盘 `sql/local_changes/`（git 仓库内**本地提交 **`a2bad74`，2026-08-09 用户决定不推送 GitHub，跨机器靠拷贝），按编号重放即可（全部幂等）：**

| 文件 | 内容 |
|---|---|
| `001_npc_190001.sql` | **Eluna Test NPC 190001 完整入库**（creature_template 模板 + creature 实体 guid 12585178 + npc_flags=1 修复） |
| `002_account_rank.sql` | admin 升 SEC_ADMINISTRATOR（原 rank=3 不够用 `.reload eluna`） |
| `003_realmlist.sql` | realmlist 初始化/修正（id=1，127.0.0.1:8090） |

```bash
MYSQL="<mariadb>/bin/mysql.exe"
"$MYSQL" -uroot -p --default-character-set=utf8 tw_world < 001_npc_190001.sql
"$MYSQL" -uroot -p --default-character-set=utf8 tw_logon < 002_account_rank.sql
"$MYSQL" -uroot -p --default-character-set=utf8 tw_logon < 003_realmlist.sql
```

> 说明：2026-08-09 运行时修复原本只记录在文档中（rank=4 / npc_flags=1 两条 UPDATE），
> NPC 190001 的建库 INSERT 此前**从未落盘**——换机器重放 SQL 会静默丢失该 NPC。
> 2026-08-09 已从本机库导出完整数据存入 `sql/local_changes/`（**本地提交 `a2bad74`，未推送**；
> 根目录 `sql/local_changes/` 为同名副本，拷贝项目目录即带上）。

**脚本修复**（已提交 GitHub `b10cbbb`，无需手动）：
- `lua_scripts/test_gossip.lua`：handler 开头 `player:GossipClearMenu()` 防菜单叠加；`GossipMenuAddItem(icon,msg,sender,action)` 用 **sender=0 + action=编号** 约定（原 1/2 误放 sender 导致选项无响应）。

## 三、需要同步的内容（重要）

| 内容 | 位置 | 同步方式 |
|---|---|---|
| **代码 + lua_scripts** | `_audit/soulcore`（Git 仓库） | ✅ **已推送 GitHub** `callmefree/soulcore-tortoise-wow` main=`b10cbbb`，家里 `git pull` 即可 |
| **本地提交（sql/local_changes + 注释）** | `_audit/soulcore/sql/local_changes/` ＋ 根目录副本 `sql/local_changes/` | ⚠️ **本地提交 `a2bad74`（SQL）/`9e2551e`（test_gossip 注释），均未推送 GitHub**（2026-08-09 用户决定保持本地不动）；拷贝项目目录或该目录即可 |
| **部署产物（二进制）** | `server/bin`（mangosd/realmd/conf/DLL） | 文件拷贝（或重新从 CI 产物解包） |
| **lua_scripts 部署版** | `server/bin/lua_scripts/`（3 个，与仓库一致） | 文件拷贝 |
| **工作记录** | `部署与Eluna移植工作记录.md`（项目根目录） | 文件拷贝 |
| **项目记忆** | `.workbuddy/memory/`（MEMORY.md + 每日日志） | 文件拷贝 |
| **本交接清单** | `切换电脑交接清单.md`（项目根目录） | 文件拷贝 |
| **用户级配置/会话/DB** | `~/.workbuddy/` | **手动拷贝**（webdav sync 已放弃，2026-08-09 用户确认；如需要会话历史/用户记忆，直接拷贝 `~/.workbuddy/` 整个目录） |
| **数据库**（四库 tw_logon/tw_world/tw_char/tw_logs） | `mariadb/mariadb-11.4.4-winx64/data` | 整库拷贝（或重放上面的 2 条 SQL） |

## 四、尚未完成/待办

- ✅ ~~workflow 补全推送~~（**已关闭 2026-08-09**：用户决定不再推送，lua_scripts 打包 + copy_runtime_dlls.ps1 改动**仅保留本地**。CI 产物不含 lua_scripts/DLL，本地部署手动补为常态。**注意：根目录 `weekly-build.yml` 与 `_audit/soulcore/.github/workflows/weekly-build.yml` 均为含补全的最新副本，拷贝项目目录即可带上；勿从 GitHub 拉取该文件（GitHub 上还是旧版）。**）
- 后续工作：见 `后续工作计划.md`（G3 回归 / 更多事件 API 测试 / 自定义内容 / 上游同步 / 可选 C++ 加固）

## 五、家里电脑恢复步骤

1. `git clone https://github.com/callmefree/soulcore-tortoise-wow.git`（或本地 pull `b10cbbb`）
2. 拷贝项目目录：`部署与Eluna移植工作记录.md`、`切换电脑交接清单.md`、`.workbuddy/memory/`、`server/`（bin+data+logs）、`mariadb/`、`客户端/`
3. 启动数据库 → **重放 `sql/local_changes/` 下 3 个 SQL（见第二节，幂等；或直接用拷贝的 data）**
4. 启动服务（PowerShell 独立进程，见工作记录第五章）：
   ```powershell
   Start-Process -FilePath "server\bin\realmd.exe" -WorkingDirectory "server\bin"
   Start-Process -FilePath "server\bin\mangosd.exe" -WorkingDirectory "server\bin"
   ```
5. 验证：日志出现 `Loaded 3 Lua scripts` + `[Eluna] Server started` + `[SuperHearthstone] 超级炉石已加载`
6. 客户端登录 admin/admin123（rank=4）→ 角色 Free → `.reload eluna` 应回 "Eluna scripts reloaded."

## 六、测试凭据速查

- GM 账号：admin / admin123（rank=4 = SEC_ADMINISTRATOR）
- 测试角色：Free（夜精灵德鲁伊，121 级，联盟）
- 测试 NPC：暴风城银行门口（-9065, 434, 93.5）"Eluna Test NPC"（entry 190001，guid 12585178）
- 数据库：root/soulcore2026（3306）；mangos/mangos（应用账号）
