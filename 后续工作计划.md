# 后续工作计划（Soulcore Turtle WoW + Eluna）

> 创建：2026-08-09 16:19 ｜ 状态：阶段二批次 1-3 已全部闭环，本文件为后续工作清单
> 前置阅读：`部署与Eluna移植工作记录.md`（背景）、`Eluna移植计划书.md`（原计划，验证标准 1-6 已 ✅）
> 同步说明：`[x]` = 完成，`[ ]` = 待做，`(阻塞：xxx)` = 需先满足条件

---

## 一、立即工作项（无阻塞，随时可开工）

### 1. G3 核心游戏回归测试（计划书验证标准唯一 ⏸ 项）
- **目标**：确认 Eluna 引擎上线后不影响核心游戏性（这是上线前的最后一道验证）。
- **测试内容**：见 `G3核心游戏回归测试清单.md`（已按可勾选清单整理，含观察日志方法）。
- **方式**：admin/Free 角色实测 + 盯 `server/logs/` 错误日志（重点：Lua 相关 ERROR、Anticheat 告警）
- **验收**：上述项目全部正常，无 Lua 引起的服务端错误

### 2. 更多事件 + Eluna API 兼容性测试（计划书 P2 遗留）
- **目标**：把引擎的常用事件逐一实测，确认触发与返回值语义（本引擎布尔语义与官方相反，每个事件都要实测，不能照抄官方文档）。
- **事件清单**：
  - [ ] `OnQuestComplete`（任务完成）
  - [ ] `OnLoot`（拾取）
  - [ ] `OnCreatureKill`（击杀）
  - [ ] `OnSpellCast`（施法）
  - [ ] `OnGossipHello` 的 GO/Item 变体（已知 item 路径引擎内部清菜单，GO 需实测）
- **产出**：每个事件的实测结论（触发点、参数、返回值语义）补入工作记录，形成《Eluna 事件实测表》，供以后写脚本直接查。

### 3. 更多自定义内容（Eluna 的最终目的）
- 按需开发，候选：
  - [ ] 自定义任务链（welcome.lua 基础上扩展）
  - [ ] 自定义 NPC 商店/兑换商（gossip 已通，可复用 190001 模式）
  - [ ] 世界事件脚本（节日、刷怪事件）
  - [ ] 超级炉石增强：更多传送点（钓鱼/专业区）、拍卖行/银行快捷键
- **规范**：写脚本前查《Eluna 事件实测表》+ 记忆中的三条 gossip 规则（GossipClearMenu / sender=0+action / item use return false）

---

## 二、例行维护（周期性）

### 4. 上游同步（每周）
- **目标**：fork（callmefree/soulcore-tortoise-wow）跟进上游 Penqle/tortoise-wow 更新。
- [ ] 每周 merge 上游 main → 解决冲突（重点是 LuaEngine 相关文件与上游改动冲突）→ 推 fork → 触发 CI 验证
- **风险**：上游改动可能与 Eluna 移植冲突；每次 merge 后必须跑 CI + 本地启动验证
- **已有保障**：weekly-build.yml 每周一 03:00 UTC 自动编译（push 也会触发）

### 5. 每周 CI 结果关注
- [ ] 周一查看自动编译结果，失败则按工作记录「编译坑速查」排查（LFT PCH、C2694 ACE patch、vmap_assembler 等）

### 6. errors.log 定期归档（避免数据校验噪音淹没真实错误）
- [ ] `server/logs/errors.log` 当前约 3.6MB，**全为启动期 creature_template 数据校验错误**（spell list / auras / equipment_id 与代码版本不匹配，Turtle 数据库数据漂移，非 Eluna 引入）
- [ ] 每次归档/清空前先 `grep -i lua errors.log` 确认无 Lua 相关 ERROR（当前 0 条，保持 0 是目标）
- [ ] 长期（可选）：核实 database_updates 是否全部应用，收敛数据漂移噪音

---

## 三、可选优化（低优先级，需要时再做）

### 6. 引擎 C++ 加固：`OnCreatureGossipHello` 自动 ClearMenus
- **动机**：对齐 item 路径（OnItemGossipHello 引擎内部有 ClearMenus），以后写 creature gossip 脚本无需手动 `player:GossipClearMenu()`。
- **成本**：改 1 处 C++（TurtleLuaEngine.cpp:24994）→ 推 CI 编译（约 1 小时）→ 换二进制部署。
- **现状**：脚本层已防御（test_gossip/super_hearthstone 均已规范），此项仅是为未来脚本省一行，**暂缓**。

---

## 四、已关闭事项（不再处理）

| 事项 | 结论 |
|---|---|
| ~~workflow 产物补全推送~~ | **2026-08-09 用户决定不再推送**。lua_scripts 打包 + copy_runtime_dlls.ps1 改动仅保留本地（根目录 `weekly-build.yml` 为最新副本）；CI 产物不含 lua_scripts/DLL，本地部署手动补为常态流程 |
| ~~webdav sync~~ | **2026-08-09 用户决定放弃**。跨机器同步：代码走 GitHub、项目/配置手动拷贝 |

---

## 五、当前建议顺序

1. **G3 核心游戏回归**（1-2 小时实测）→ 补齐计划书最后一项验证，Eluna 移植正式收尾
2. **事件实测表**（第 2 项）→ 为后续所有脚本开发打基础
3. 之后按需做自定义内容（第 3 项）；上游同步按周例行（第 4 项）

---

_维护：每完成一项勾选一项；新发现的工作项追加到对应区块。_
